{
  "name": "@theWallProject/common",
  "version": "1.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm run generate-schema && pnpm run validate-schema && pnpm run test && tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-schema": "ts-node scripts/generate-schema.ts",
    "validate-schema": "ts-node scripts/validate-schema.ts"
  },
  "dependencies": {
    "tldts": "^7.0.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jsonschema": "^1.4.1",
    "prettier": "3.6.2",
    "vitest": "^1.0.0",
    "zod-to-json-schema": "^3.25.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
