{
  "name": "@theWallProject/common",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run",
    "test:watch": "vitest",
    "generate-schema": "ts-node scripts/generate-schema.ts"
  },
  "dependencies": {
    "tldts": "^7.0.8",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vitest": "^1.0.0",
    "zod-to-json-schema": "^3.23.5"
  },
  "publishConfig": {
    "access": "public"
  }
}
